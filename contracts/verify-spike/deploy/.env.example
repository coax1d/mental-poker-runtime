# Copy to `.env` and fill in. `.env` is gitignored.
#
# Populated by `npm run gen-account`.
PASSET_HUB_MNEMONIC=
PASSET_HUB_ADDRESS=

# Optional override — defaults to wss://testnet-passet-hub.polkadot.io.
PASSET_HUB_WS=wss://testnet-passet-hub.polkadot.io
